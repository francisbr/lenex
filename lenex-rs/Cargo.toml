[package]
name = "lenex"
version = "0.1.0"
edition = "2024"
description = "Parse and generate lenex documents."
license = "MIT"
repository = "https://github.com/francisbr/lenex-rs"

[dependencies]
thiserror = { version = "2.0", default-features = false, features = ["std"] }
